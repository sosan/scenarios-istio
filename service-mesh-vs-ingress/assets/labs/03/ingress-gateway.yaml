apiVersion: networking.istio.io/v1alpha3
kind: Gateway
metadata:
  name: mock-apps-gateway
spec:
  selector:
    istio: istio-ingressgateway #ingressgateway
  servers:
  - port:
      name: http
      number: 80
      protocol: HTTP
    hosts:
    - istio.lab-01.com
---
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: httpbin-app
spec:
  hosts:
  - istio.lab-01.com
  gateways:
  - mock-apps-gateway.istio-ingress.svc.cluster.local
  http:
  - match:
    - uri:
        exact: /
    route:
    - destination:
        host: backend-api
        port:
          number: 8080
# 
# 
# apiVersion: networking.istio.io/v1alpha3
# kind: Gateway
# metadata:
#   name: my-gateway
# spec:
#   selector:
#     istio: ingressgateway # use istio default ingress gateway
#   servers:
#   - port:
#       name: http
#       number: 80
#       protocol: HTTP
#     hosts:
#     - "*"
#   - port:
#       name: https
#       number: 443
#       protocol: HTTPS
#     tls:
#       mode: SIMPLE
#       privateKey: /etc/istio/ingressgateway-certs/tls.key
#       serverCertificate: /etc/istio/ingressgateway-certs/tls.crt
#     hosts:
#     - "*"

